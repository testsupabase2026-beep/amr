# NAMAA Analytics Agent
# Pinned to the verified working runtime (env: qwen3-tts) for reproducible deploys.
# CPU-only torch wheel keeps the image small (~1.2 GB saved vs CUDA) — required for
# free-tier hosts with no GPU. Local dev may use a CUDA torch; that's fine.
--extra-index-url https://download.pytorch.org/whl/cpu

# ── PyTorch (CPU only) ────────────────────────────────────────────────────────
torch==2.5.1

# ── Gradio UI ─────────────────────────────────────────────────────────────────
gradio==6.4.0

# ── LangChain (1.x — do NOT loosen; 0.x resolves to incompatible APIs) ─────────
langchain==1.2.10
langchain-core==1.2.28
langchain-community==0.4.1
langchain-groq==1.1.2
langchain-huggingface==1.2.0
langchain-text-splitters==1.1.1

# ── Vector store & embeddings ─────────────────────────────────────────────────
faiss-cpu==1.13.2
sentence-transformers==5.2.3
transformers==4.57.3
huggingface-hub==0.36.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.47
psycopg2-binary==2.9.11

# ── Data processing ───────────────────────────────────────────────────────────
pandas==2.3.3
numpy==2.3.5
openpyxl==3.1.5
tabulate==0.10.0

# ── Charts & export ───────────────────────────────────────────────────────────
plotly==6.5.2
kaleido==1.2.0        # PDF PNG export; needs headless Chromium — wrapped in try/except
reportlab==4.4.10

# ── Arabic text support ───────────────────────────────────────────────────────
arabic-reshaper==3.0.0
python-bidi==0.6.7

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.1

# ── REST API (api.py — alternative to the Gradio UI) ──────────────────────────
fastapi==0.128.0
uvicorn==0.40.0
python-multipart==0.0.22
